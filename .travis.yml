sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t richard/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true richard/docker-react npm run test
